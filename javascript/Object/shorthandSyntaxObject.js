/**
 * shorthand Syntax Object
 */


const username = "Ricky";
const age = 30;
let address = "Shantinagar alias kurri";


let userDetails = {
    username,
    age,
    address
}

console.log(userDetails);
console.log(`Hello guys my name is ${userDetails.username} and my age is ${userDetails.age} and my address is ${userDetails.address}`);